package middleware

import "context"

// Auth validates the JWT on incoming requests.
// TODO: implement per TECH-STACK-DAN-PERSIAPAN.md §4 (golang-jwt/jwt/v5).
func Auth(ctx context.Context) error {
	return nil
}
